# Set this to your deployed backend URL for production builds.
# Example: VITE_API_URL=https://your-backend-api.example.com
# If not set, the app derives the URL from window.location (works for local dev at localhost:8000).
VITE_API_URL=
